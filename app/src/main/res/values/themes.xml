<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ToDoApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>


    <style name="MyEditTextStyle">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>


    <style name="MyCustomCheckboxStyle" parent="Widget.AppCompat.CompoundButton.CheckBox">
        <!-- Seçili durumda olan checkbox rengi -->
        <item name="colorControlActivated">@color/customCheckedColor</item>
        <!-- Seçili olmayan (boş) checkbox rengi -->
        <item name="colorControlNormal">@color/customUncheckedColor</item>
        <!-- Tıklanabilirlik rengi (örneğin, tıklandığında gözükür) -->
        <item name="colorControlHighlight">@color/customHighlightColor</item>
    </style>

    <style name="Theme.ToDoApp" parent="Base.Theme.ToDoApp" />
</resources>